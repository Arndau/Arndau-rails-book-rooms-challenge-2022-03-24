<%# <div class="showlinkback">
<%= link_to rooms_path do %>
  <%# <p><i class="fas fa-arrow-left"></i> Back to index</p> %>
  <%# <% end %>
<%# </div> %>
<button type="button" class="btn btn-primary"><i class="fas fa-arrow-left"></i> Back to index</button>
<br>
<div class="showcontent">
  <img src="https://www.nycinsiderguide.com/wp-content/uploads/2018/05/pod-39-hotel-nyc-bunk-beds.jpg">
  <div class="showinfo">
    <h2><%=@room.hotel.name%></h2>
    <h5>Price : <%= @room.price_per_night%>$/night</h5>
    <h5>Address : <%= @room.hotel.address%></h5>
  <%= simple_form_for [@room, Booking.new] do |f| %>
  <div class="d-flex">
    <%= f.input :sarts_at, as: :string, required: false, input_html: { id: "range_start" } %>
    <%= f.input :ends_at, as: :string, required: false, input_html: { id: "range_end" } %>
  </div>
      <%= f.button :submit, "Book now", class: "btn btn-primary" %>
  <% end %>
  </div>
</div>

<%#= render "form", booking: @booking %>
